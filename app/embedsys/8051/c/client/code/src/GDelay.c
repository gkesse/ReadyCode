//===============================================
#include "GDelay.h"
//===============================================
void GDelay_ms(uint _ms) {
    uint i, j;
    for(i = 0;i < _ms; i++) {
        for(j = 0; j < 125; j++);
    }
}
//===============================================
