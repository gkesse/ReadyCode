//===============================================
function phone_slide_bar_dot_onclick(obj, index) {
    var lPhone = new GPhone();
    lPhone.showSlides(index);
}
//===============================================
