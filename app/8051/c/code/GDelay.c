//===============================================
#include "GDelay.h"
//===============================================
#define TIME_1_MS (125)
//===============================================
void GDelay_ms(uint ms) {
    uint i, j;
    for(i = 0; i < ms; i++) {
        for(j = 0; j < TIME_1_MS; j++);
    }
}
//===============================================
